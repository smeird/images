server {
    listen 80;
    listen [::]:80;
    server_name images.smeird.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name images.smeird.com;

    root /var/www/images/public;
    index index.php;
    client_max_body_size 160m;

    ssl_certificate /etc/letsencrypt/live/images.smeird.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/images.smeird.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        try_files $uri =404;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_pass unix:/run/php/php8.5-fpm-images.sock;
    }

    location ~ /\. {
        deny all;
    }
}
